[project]
name = "stiq"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.13.13"
dependencies = [
    "pyinstaller>=6.20.0",
    "pytz>=2026.2",
    "setuptools>=82.0.1",
]

[project.optional-dependencies]
yfinance = [
    "yfinance>=1.3.0",
]
